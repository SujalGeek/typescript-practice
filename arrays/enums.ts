// enum
const enum SeatChoice {
  AISLE =10 ,
  MIDDLE = 22,
  WINDOW
}

const hcSeat = SeatChoice.AISLE;

const enum SeatChoice2 {
  AISLE ="aisle" ,
  MIDDLE = 22,
  WINDOW
}
enum SeatChoice3 {
  AISLE ="aisle" ,
  MIDDLE = "middle",
  WINDOW = "window",
  CUSTOM = "custom"
}